import {API_METHOD as METHOD , BASE_URL} from "../containers"
const API_SCHEME = {
    ITEM : {
        GET_ITEM : {
            url : `${BASE_URL}/item`,
            method : METHOD.GET
        },
      
    }
}

export default API_SCHEME